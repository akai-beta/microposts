<h2>user list</h2>
<% if users.any? %>
  <ul class="media-list">
    <% users.each do |user| %>
      <li class="media">
        <div class="media-left">
          <img class="media-object media-rounded" src="<%= gravatar_url(user, { size: 50 }) %>" alt="">
        </div>
        <div class="media-body">
          <div>
            <%= user.name %>
          </div>
          <div>
            <p><%= link_to 'View Profile', user_path(user) %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
<%= paginate users %>
<% end %>

